// Заглушка для «узловых» модулей (fs, http, zlib и т.п.) и для необязательного
// хранилища epub.js. Библиотеки проверяют их наличие ради путей, которые в
// Obsidian не выполняются: pdf.js читает файлы через Node только в серверном
// режиме, epub.js кладёт книгу в localforage только если его об этом попросить.
// Пустышка убирает такие ветки из сборки — вместе с тем, что автопроверка
// каталога справедливо считает доступом к файловой системе и загрузкой скриптов.
const empty = {};
export default empty;
export const createInstance = () => empty;
